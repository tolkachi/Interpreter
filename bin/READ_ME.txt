To run the program copy the files of the compiled code in bin folder to the local computer,
open command prompt, go to the path of folder with the files and type command "java Interpreter_Main test1.txt test2.txt".
After running the command, you will see the program input to the interpreter and the result of the calculations done by the interpreter.
